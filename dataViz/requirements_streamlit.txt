# Dependências do Streamlit Visualizador de Processos (Cloud Run)
# Inclui apenas o necessário para dataViz/streamlit_grafo.py

# >=1.49.0 necessário para suporte nativo a st.mermaid_chart (usado em vez de mermaid.ink/CDN)
# Piso conservador; instalado localmente 1.60.0. Ajustar se a versão exata de
# introdução do st.mermaid_chart for confirmada no changelog oficial do Streamlit.
streamlit>=1.49.0

# Leitura do Postgres (N0-N4 + Mermaid) via Cloud SQL Python Connector
cloud-sql-python-connector[pg8000]>=1.12.0

# Autenticação GCP (ADC no Cloud Run / service account local)
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
